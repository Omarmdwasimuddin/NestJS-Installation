# NestJS কি?

NestJS হলো একটি ফ্রেমওয়ার্ক, যা efficient এবং scalable Node.js server-side application বানানোর জন্য ব্যবহৃত হয়।

- Progressive JavaScript ব্যবহার করে
- সম্পূর্ণ TypeScript support সহ তৈরি (তবে pure JavaScript দিয়েও কাজ করা যায়)
- OOP, FP, এবং FRP — এই তিন ধরনের প্যারাডাইম একসাথে combine করে
- মূলত Express (default) অথবা Fastify এর উপর চলে, এবং এদের API সরাসরি ব্যবহার করার সুযোগও দেয়

## NestJS কেন বানানো হলো

Node.js এর কারণে JavaScript ফ্রন্টএন্ড এবং ব্যাকএন্ড — দুই জায়গাতেই মূল ভাষা হয়ে উঠেছে। Node এর জন্য অনেক library আর tool থাকলেও, এদের বেশিরভাগই একটা মূল সমস্যার সমাধান দেয়নি — সেটা হলো **architecture**।

NestJS ঠিক এই জায়গাতেই কাজ করে — এটি একটি রেডিমেড architecture দেয় (Angular দ্বারা অনুপ্রাণিত), যার মাধ্যমে এমন application বানানো যায় যেগুলো:

- সহজে testable
- Scalable
- Loosely coupled (কম নির্ভরশীল অংশ নিয়ে গঠিত)
- সহজে maintain করা যায়
